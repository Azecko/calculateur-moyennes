{
  "name": "calculateur-moyennes-api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test-dev": "cross-env NODE_ENV=test jest --watchAll --verbose",
    "test": "cross-env NODE_ENV=test jest",
    "runapi": "npm i && node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql2": "^2.3.3",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
